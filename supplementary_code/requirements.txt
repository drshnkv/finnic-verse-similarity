# Tested with Python 3.10.18 (conda).
numpy==1.26.4
scipy==1.14.1
torch==2.2.2
sentence-transformers==5.3.0
transformers==4.56.2
faiss-cpu==1.13.2
psutil==7.2.2
sparse-dot-topn==1.2.0   # optional speed-up; verse_similarity_common.py falls back to pure scipy if absent

# Optional, only for non-default code paths (imported lazily; the reported
# results do not depend on them):
#   onnxruntime==1.23.2   # build_verse_sentence_embedding.py ONNX encoder path

# NOTE: EstNLTK 1.7.4 was used UPSTREAM to lemmatize the corpus, but is NOT
# required by these scripts — they consume the pre-lemmatized verse registry.
# scikit-learn is likewise not required (TF-IDF is implemented directly).
